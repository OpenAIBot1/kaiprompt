import 'package:flutter/material.dart';

class PromptMainEditingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Prompt Main Editing Screen')),
    );
  }
}
