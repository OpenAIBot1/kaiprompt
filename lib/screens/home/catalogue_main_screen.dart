import 'package:flutter/material.dart';

class CatalogueMainScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Catalogue Main Screen')),
    );
  }
}
