import 'package:flutter/material.dart';

class ServiceAgreementScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Service Agreement Screen')),
    );
  }
}
